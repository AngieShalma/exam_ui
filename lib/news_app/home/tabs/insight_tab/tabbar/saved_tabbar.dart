import 'package:flutter/cupertino.dart';

class savedTabbar extends StatelessWidget {
  const savedTabbar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
