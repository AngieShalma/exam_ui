import 'package:flutter/cupertino.dart';

class newsTabbar extends StatelessWidget {
  const newsTabbar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
