import 'package:flutter/material.dart';

class tab2Workout extends StatelessWidget {
  const tab2Workout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
