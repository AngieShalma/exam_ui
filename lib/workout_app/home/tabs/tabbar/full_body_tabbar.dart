import 'package:flutter/cupertino.dart';

class fullBodyTabbar extends StatelessWidget {
  const fullBodyTabbar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
