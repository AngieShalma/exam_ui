import 'package:flutter/cupertino.dart';

class lowerTabbar extends StatelessWidget {
  const lowerTabbar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
