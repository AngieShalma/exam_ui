import 'package:flutter/material.dart';

class tab3Workout extends StatelessWidget {
  const tab3Workout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
