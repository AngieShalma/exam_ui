import 'package:flutter/material.dart';

class tab4Workout extends StatelessWidget {
  const tab4Workout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
